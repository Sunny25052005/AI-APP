{
  "entities": {
    "users": {
      "fields": ["name", "email"]
    },
    "products": {
      "fields": ["name", "price"]
    },
    "orders": {
      "fields": ["productId", "userId", "quantity"]
    }
  }
}
